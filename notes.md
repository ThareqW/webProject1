# Functional Requirements and Notes

Light/Dark mode toggle
- What HTML markup
- Switching between light and dark mode
- Set the toggle to allow 3 values: light, dark, system (take system by default but can be change using toggle)

CSS variable (custom properties) -> Use javascript to set the CSS variable

Prefer-color-scheme media query

Accessibility
- Use corret heading tags
- apply screenreader-only text for card title/username (make a text hidden and can only be accessible using screenreader)